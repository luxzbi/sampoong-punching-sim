# methodology.md — 무슨 변수와 가정을 사용했는가

> **교육용 단순화 모델입니다. 실제 구조 안전진단을 대체하지 않습니다.**

이 문서는 "이 시뮬레이터가 무엇을 계산하고, 무엇을 계산하지 않는가"를 정리한 것입니다.
식의 출처는 [`source-notes.md`](./source-notes.md), 한계는 [`limitations.md`](./limitations.md)를 보세요.

---

## 1. 탐구 질문

> **구조가 어떻게 달랐다면 슬래브-기둥 접합부의 펀칭전단과 그로 인한 연쇄붕괴 위험을 줄일 수 있었는가?**

이 질문은 "삼풍백화점이 왜 무너졌는가"를 전부 설명하려는 것이 아닙니다.
붕괴 조사에서 지적된 여러 요인 중 **구조적 요인 하나(펀칭전단)** 만 떼어 내어,
변수를 바꾸면 위험도가 어느 방향으로 움직이는지를 확인하는 것이 목적입니다.

---

## 2. 모델링 대상

- 무량판(플랫플레이트) **2 × 2 bay** 일부 (기둥 9개, 슬래브 패널 4개)
- 기본값은 기둥머리 드롭패널이 **없는** 상태이며, 드롭패널을 두었을 때와 비교할 수 있음
- 그중 **중앙 내부기둥 1개**의 슬래브-기둥 접합부를 검토 대상으로 삼음
- 건물 전체, 층수, 지하구조, 벽체, 코어는 모델링하지 않음

내부기둥을 고른 이유는 부담면적이 가장 크고, 4방향 대칭이라 단순한 등분포 가정이
비교적 덜 왜곡되기 때문입니다.

---

## 3. 사용자가 조절하는 변수

| # | 변수 | 범위 | 계산에서의 역할 |
|---|---|---|---|
| 1 | 기둥 직경 D | 500 ~ 1000 mm | 위험단면 둘레 `b₀ = π(D+d)`, 기둥 단면적 |
| 2 | 콘크리트 압축강도 f'c | 15 ~ 40 MPa | 전단강도 `∝ √f'c`, 기둥 축력 저항 |
| 3 | 기둥부 유효깊이 d₁ | 300 ~ 500 mm | 위험단면 면적 `b₀·d`, 크기효과계수 |
| 4 | 상부 인장철근비 ρ | 0.2 ~ 2.0 % | **EC2 식에서만** 저항에 반영 |
| 4' | 기둥 주철근 개수 | 4 ~ 32 (HD22) | 기둥 축력 저항(보조 지표) |
| 5 | 고정하중 (마감·설비) | 0 ~ 10 kPa | 요구 전단력 |
| 6 | 활하중 | 0 ~ 12 kPa | 요구 전단력 |
| 7 | 국부 추가하중 | 0 ~ 12 kPa | 요구 전단력 (고정하중으로 취급) |
| 8 | 드롭패널 유무 / 크기 / 두께 | 1500~5000 mm, 0~250 mm | 두 번째 위험단면, 자중, d₂ |
| 9 | 펀칭전단 보강 유무 / 보강량 | 0 ~ 1 (상대비율) | 전단강도 상한 |
| 10 | 용도 | 판매장 / 식당가 / 사무실 / 창고 | 고정하중·활하중 대표값을 자동 적용 |

**추가로 넣은 변수** (원래 목록에는 없지만 계산에 반드시 필요해서 노출한 것)

| 변수 | 이유 |
|---|---|
| 경간 X, Y | 부담면적을 정해야 전단력을 계산할 수 있음. 값을 확인하지 못해 가정값 |
| 지지 층 수 | 기둥 축력 검토용 |
| 불균형 모멘트 할증 계수 | 정밀 계산을 하지 않는 대신 사용자가 직접 할증할 수 있게 함 |
| 적용 기준식 (ACI / EC2) | 기준에 따라 답이 달라진다는 점 자체를 보여주기 위함 |
| 하중 조합 (설계 / 사용) | "설계기준 만족 여부"와 "실제로 서 있었는가"는 다른 질문이기 때문 |
| 크기효과계수 λs 적용 여부 | 1995년 기준과 현행 기준의 차이를 보기 위함 |

---

## 4. 계산 흐름

```
입력 변수
   │
   ├─ 기하 : 슬래브 두께 h = d₂ + 40mm(피복 가정),  d₂ = d₁ − 드롭패널 두께
   │
   ├─ 하중 : w = 1.2(D + 국부추가) + 1.6L   또는   1.0(D + 국부추가) + 1.0L
   │          D = 슬래브 자중(두께×24) + 드롭패널 자중(평균환산) + 마감·설비
   │
   ├─ 요구 : V = w × (부담면적 − 위험단면 내부면적)         [kN]
   │          Vu = V × 불균형모멘트 할증                     [kN]
   │          vu = Vu / (b₀ · d)                            [MPa]
   │
   ├─ 저항 : ACI  vn = λs·min(0.33, …)·√f'c        (전단보강 없을 때)
   │          EC2  v_Rd,c = (0.18/1.5)·k·(100ρl·fck)^(1/3)
   │
   ├─ 저항(힘) : φVn = φ · vn · b₀ · d                      [kN]
   │
   └─ 판정 : D/C = Vu / φVn = vu / (φ·vn),  두 위험단면 중 큰 쪽이 지배
```

검토하는 위험단면은 다음과 같습니다.

1. **기둥 주변** — 기둥면에서 `d/2`(ACI) 또는 `2d`(EC2) 떨어진 둘레
2. **드롭패널 외곽** — 드롭패널이 끝나 슬래브가 얇아지는 위치
3. **기둥면 둘레** — EC2 선택 시 최대 전단응력 상한 검토

---

## 5. 위험도 구간

| D/C | 표시 | 의미 |
|---|---|---|
| 0.0 ~ 0.7 | 상대적으로 낮음 | 이 모형 안에서 여유가 있는 편 |
| 0.7 ~ 1.0 | 주의 | 여유가 크지 않아 조건이 조금만 나빠져도 1.0을 넘음 |
| 1.0 이상 | 파괴 위험 | 이 모형의 가정 안에서 저항보다 요구가 큼 |

**이 구간 구분도 교육용으로 정한 것이며, 실제 안전 판정 기준이 아닙니다.**
실제 설계에서는 D/C ≤ 1.0 을 만족해야 하지만, 그 값이 곧 안전을 보장하는 것도 아닙니다.

---

## 6. 두 가지 하중 조합을 함께 보여주는 이유

| 모드 | 조합 | φ | 답하는 질문 |
|---|---|---|---|
| 설계 검토 | 1.2D + 1.6L | 0.75 | "이 설계가 현행 기준을 만족하는가?" |
| 사용하중 추정 | 1.0D + 1.0L | 1.00 | "실제 사용 상태에서 저항보다 요구가 컸는가?" |

설계 검토 값에는 안전율이 포함되어 있으므로 항상 더 큽니다.
D/C(설계) > 1 이라고 해서 반드시 즉시 무너진다는 뜻이 아니고,
D/C(사용) > 1 이면 이 모형의 가정 안에서는 파괴가 예상된다는 뜻입니다.

---

## 7. 3D 시각화가 표현하는 것과 표현하지 않는 것

**표현하는 것**

- 슬래브·원형기둥·드롭패널·슬래브-기둥 접합부의 상대적인 크기 (입력값에 비례)
- 상부 철근의 개념적 배치 (실제 배근도가 아님)
- 하중을 나타내는 아래 방향 화살표 (하중 크기에 따라 길이 변화, 국부 추가하중은 붉은색)
- 접합부 주변의 **원형·방사형 균열** — D/C 가 커질수록 개수·범위·진하기가 증가
- 펀칭전단 파괴 : 기둥이 슬래브를 뚫고 원뿔대가 기둥에 남는 형태
- 연쇄붕괴 : 파괴된 접합부의 하중이 인접부로 재분배되어 시간차를 두고 무너지는 개념

**표현하지 않는 것**

- 실제 변위·처짐량, 응력 분포, 균열 폭 (모두 정성적 표현)
- 붕괴의 실제 순서·속도·파편 거동 (동역학 해석이 아님)
- 철근의 실제 정착·이음·항복 거동

균열 애니메이션은 계산된 D/C 값에 연동되지만, **균열 자체를 역학적으로 계산한 것은 아닙니다.**

---

## 8. 탐구 모드 (One Variable at a Time)

기준 모델을 Scenario A 로 고정하고 다음을 각각 한 번씩 적용합니다.

1. 기둥 600 → 800 mm
2. 기둥부 유효깊이 360 → 410 mm
3. 콘크리트 18.4 → 21 MPa
4. 국부 추가하중 제거
5. 용도 되돌리기 (식당가 → 판매장)
6. 하중만 원설계 수준으로 (5 + 4 동시, 구조 단면은 그대로)
7. 드롭패널 신설 (없음 → 3000×100 mm, d₁ +100 mm)
8. 펀칭전단 보강 추가
9. 위 모두 동시 적용

6번은 엄밀히는 변수 두 개를 함께 바꾼 것이지만, "같은 건물이 한동안 서 있다가
무너졌다"는 과정을 이해하는 데 핵심이 되는 조합이라 따로 두었습니다.

한 번에 하나씩 바꾸는 이유는, 여러 변수를 동시에 바꾸면 **어떤 변화가 결과를 만들었는지
알 수 없기 때문**입니다. 다만 변수들 사이에 상호작용이 있어서
(예: 유효깊이를 키우면 자중도 늘어남) 개별 변화량의 합이 전체 변화량과 일치하지 않습니다.

---

## 9. 재현 방법

```bash
npm install
npm run dev      # 개발 서버 (브라우저에서 확인)
npm run build    # 타입 검사 + 프로덕션 빌드
npm run sanity   # 계산값 검증 스크립트 (콘솔 출력)
```

모든 계산은 브라우저에서 수행되며, 외부 API 나 서버를 사용하지 않습니다.

---

## 10. 화면에 실시간으로 표시되는 값

시뮬레이션 탭 오른쪽에 항상 다음 세 가지가 함께 표시됩니다.

```
Punching Shear Demand    Vu  = 2,289 kN
Punching Shear Capacity  φVn = 1,044 kN
Demand / Capacity Ratio  D/C = Vu / φVn = 2,289 / 1,044 = 2.19
```

그 아래 **계산 과정** 카드에는 실제 숫자를 대입한 여덟 줄이 그대로 나옵니다.

```
① 위험단면 둘레   b₀ = π(D + d₁) = π(600 + 360)          3,016 mm
② 유효깊이        d₁ (드롭패널 포함)                        360 mm
③ 단위 전단강도   φvc = 0.75 × 0.33·λs·√f'c, λs = 0.905   0.961 MPa
④ 설계 전단내력   φVn = φvn × b₀ × d                      1,044 kN
⑤ 등분포하중      w = 1.2D + 1.6L (계수하중)               28.52 kPa
⑥ 부담면적        A = 9.0 × 9.0 − A(위험단면 내부)         80.28 m²
⑦ 소요 전단력     Vu = w × A                              2,289 kN
⑧ 판정            D/C = Vu / φVn                          2.19
```

변수를 하나 바꾸면 오른쪽 아래 설명 카드에 **요구와 저항 중 무엇이 움직였는지**가
자동으로 서술됩니다. 예를 들어 기둥 직경을 600 → 800 mm 로 바꾸면 이렇게 표시됩니다.

> 저항 쪽이 움직입니다 : 설계 전단내력 φVn +20.8% (Vu 는 거의 그대로) → D/C -17.6%
>
> 기둥 직경이 증가하여 슬래브-기둥 접합부의 임계둘레 b₀ = π(D + d) 가 길어졌고,
> 전단이 전달되는 위험단면 면적(b₀·d)이 커져 펀칭전단 저항능력 φVn 이 증가했습니다.
> 기둥 압축 단면적이 아니라 임계둘레가 커지는 것이 펀칭전단에서 핵심입니다.

이 서술은 정해진 문장을 고르는 것이 아니라, **그 변수만 바꾼 모델을 다시 계산해서**
Vu 와 φVn 의 변화율을 구한 뒤 만들어집니다.

---

## 11. 계산과 애니메이션의 구분

이 프로그램에서 **숫자와 그림은 계산 경로가 다릅니다.** 반드시 구분해서 읽어야 합니다.

| | 무엇인가 | 근거 |
|---|---|---|
| Vu, φVn, D/C, 모든 표와 그래프 | 계산 결과 | ACI 318-19 22.6 / EN 1992-1-1 6.4 |
| 균열 표현(원형·방사형) | D/C 값에 연동한 **표현** | 균열 역학 계산 아님 |
| 펀칭 파괴 · 연쇄붕괴 애니메이션 | **개념 시각화** | 동적 해석 아님. 하중 재분배 개념만 보여 줌 |

붕괴 애니메이션은 D/C 가 1.0 미만일 때도 재생할 수 있으며, 그 경우 화면에
"이 조건에서 붕괴가 일어난다는 뜻이 아니다"라는 경고가 함께 표시됩니다.

---

## 12. 붕괴 회피 실험 (탭 2)

"어떻게 하면 무너지지 않을 수 있었는가"를 직접 실험하는 탭입니다.
세 가지 실험 모두 위와 **같은 펀칭전단 식**을 조건만 바꿔 반복 계산한 것입니다.
계산 방법의 자세한 내용은 `source-notes.md` 7장에 있습니다.

1. **2×2 요인 실험** — 시공 조건(실제 / 구조계산서) × 하중 조건(실제 / 원설계)
2. **누적 개선 경로** — 지적된 항목을 하나씩 되돌릴 때 D/C 가 언제 1.0 아래로 내려가는가
3. **임계값 탐색** — 변수 하나만으로 목표 D/C 를 만들려면 얼마가 필요한가 (이분법)

기본 설정에서의 결과는 다음과 같습니다. (사용하중 기준, 교육용 값)

| | 하중 : 실제 사용 | 하중 : 원설계 |
|---|---|---|
| **시공 : 실제** | **1.28** (파괴 위험) | 0.93 |
| **시공 : 구조계산서** | 0.91 | 0.67 |

이 모형 안에서는 네 칸 중 한 칸만 1.0 을 넘습니다. 즉 **시공 조건과 사용 조건 중
어느 한쪽만 원래 계획대로였어도 이 접합부의 D/C 는 1.0 아래였다**는 결과가 나옵니다.
다만 이것은 접합부 하나에 대한 단순화 계산이며, 실제 사건의 원인을 확정하는 근거가 될 수 없습니다.
