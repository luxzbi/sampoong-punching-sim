# source-notes.md — 사용한 식과 수치의 출처

> **이 프로그램은 교육용 단순화 모델이며, 실제 구조 안전진단을 대체하지 않습니다.**
> 아래 기록은 "이 프로그램이 무엇을 근거로 계산하는가"를 투명하게 남기기 위한 것입니다.
> 공식을 임의로 만들지 않았으며, 코드 기준식을 단순화한 부분은 모두 명시했습니다.

---

## 1. 사용한 계산식

### 1.0 단위 규약

```
길이 mm   면적(부담면적) m2   하중 kPa (= kN/m2)   응력 MPa (= N/mm2)   힘 kN
```

화면에는 **힘 단위(kN)** 로 Vu 와 phiVn 을 표시하고, 응력(MPa)도 함께 보여 줍니다.
두 방식의 D/C 는 같은 b0·d 로 나누고 곱한 값이라 항상 동일합니다.

```
D/C = vu / (phi·vn) = Vu / (phi·Vn),   phi·Vn = phi·vn × b0 × d
```

### 1.1 소요 전단력 (demand)

내부기둥 1개가 부담하는 등분포하중으로부터 접합부 전단력을 구합니다.

```
A_trib = spanX × spanY                      … 내부기둥 부담면적 (m²)
V      = w × (A_trib − A_in)                … 위험단면이 부담하는 전단력 (kN)
Vu     = V × β_m                            … 소요 전단력 (kN)
vu     = Vu × 1000 / (b₀ × d)               … 소요 전단응력 (MPa)
φVn    = φ · vn · b₀ · d / 1000             … 설계 전단내력 (kN)
```

- `A_in` : 위험단면 안쪽 면적 (그 부분의 하중은 기둥이 직접 받으므로 제외)
- `β_m` : 불균형 모멘트 할증 계수 (사용자 입력, 기본값 1.00)

구현 위치: `src/model/punching.ts` → `makeChecks()`

### 1.2 ACI 318-19 방식 (기본값)

| 항목 | 식 | 조항 |
|---|---|---|
| 위험단면 위치 | 기둥면에서 `d/2` | ACI 318-19 22.6.4 |
| 원형기둥 둘레 | `b₀ = π(D + d)` | 22.6.4 |
| 콘크리트 전단강도 | `vc = λs·λ·min(0.33, 0.17(1+2/β), 0.083(2+αs·d/b₀))·√f'c` | 22.6.5.2 |
| 크기효과계수 | `λs = √(2 / (1 + 0.004d)) ≤ 1.0` | 22.5.5.1.3 |
| 강도감소계수 | `φ = 0.75` (전단) | 21.2.1 |

- 원형 기둥이므로 `β = 1`(장변/단변비), 내부기둥이므로 `αs = 40`.
- 보통중량 콘크리트이므로 `λ = 1.0`.
- **원형 기둥 위험단면 처리** : ACI 는 원형 기둥을 "같은 면적의 정사각형"으로 이상화하는 것도
  허용하지만, 이 프로그램은 물리적으로 이해하기 쉬운 `b₀ = π(D + d)` 를 사용합니다.
  두 방식의 결과는 몇 % 수준에서 다릅니다.
- **크기효과계수 λs** 는 1995년 당시 기준에는 없던 최신 조항이므로 화면에서 끌 수 있게 했습니다.
  끄면 `λs = 1.0` 이 되어 1990년대식 `vc = 0.33√f'c` 형태가 됩니다.

### 1.3 드롭패널이 있을 때의 두 번째 위험단면

드롭패널이 끝나고 슬래브가 다시 얇아지는 위치에서도 펀칭전단을 검토해야 합니다.

```
정사각 드롭패널(한 변 L_d), 바깥 슬래브 유효깊이 d₂
b₀ = 4 × (L_d + d₂)
A_in = (L_d + d₂)²
```

두 위험단면 중 D/C 가 큰 쪽을 **지배 단면**으로 표시합니다.

### 1.4 전단보강이 있을 때 (단순화한 부분 — 주의)

ACI 318-19 는 전단보강근이 있으면 `vn = vc + vs`, `vs = Av·fyt/(b₀·s)` 로 계산하고,
콘크리트 분담을 `0.17λs√f'c` 로 낮추며, 스터럽의 경우 전체를 `0.5√f'c` 로 제한합니다.

이 프로그램은 **실제 배근량을 입력받지 않고** 0~1 의 "보강량 비율"로 단순화했습니다.

```
vc = 0.17 · λs · √f'c
vs = ratio × 0.33 · √f'c            ← 이 부분이 단순화
vn = min(vc + vs, 0.5·√f'c)
```

즉 `ratio = 1.0` 이면 대략 스터럽 보강의 상한 근처가 되도록 맞춘 **교육용 표현**입니다.
실제 설계에서는 스터드/스터럽의 단면적, 간격, 배치 범위를 모두 계산해야 합니다.

### 1.5 Eurocode 2 방식 (보조 옵션)

ACI 식에는 상부철근비가 들어가지 않기 때문에, "철근량"이라는 변수의 영향을 보려면
EC2 식이 필요합니다. 그래서 두 기준을 모두 넣었습니다.

| 항목 | 식 | 조항 |
|---|---|---|
| 기본둘레 | 기둥면에서 `2d`, 원형기둥 `u₁ = π(D + 4d)` | EN 1992-1-1 6.4.2 |
| 전단강도 | `v_Rd,c = (0.18/γc)·k·(100·ρl·fck)^(1/3) ≥ v_min` | 6.4.4 (1) |
| 크기계수 | `k = 1 + √(200/d) ≤ 2.0` | 6.4.4 (1) |
| 최소강도 | `v_min = 0.035·k^1.5·fck^0.5` | 6.4.4 (1) |
| 기둥면 상한 | `v_Rd,max = 0.5·ν·fcd`, `ν = 0.6(1 − fck/250)` | 6.4.5 (3) |

- `γc = 1.5`, `ρl ≤ 0.02` 로 제한.
- **전단보강 반영은 크게 단순화**했습니다. EC2 6.4.5 의 정식(`0.75·v_Rd,c + 1.5(d/sr)·Asw·fywd,ef/(u₁·d)`)
  대신 `v_Rd,cs = 0.75·v_Rd,c + ratio × 1.0·v_Rd,c` 를 사용합니다. 정량적 설계값이 아니라
  "보강하면 저항이 커진다"는 방향성을 보여주기 위한 표현입니다.

### 1.6 기둥 축력 (보조 지표)

펀칭전단과 별개로, 구조계산서 16-HD22 → 도면 8-HD22 변경의 의미를 보기 위한 참고 지표입니다.

```
Ast     = (주철근 개수) × 387 mm²          … D22 공칭단면적
Pn,max  = 0.80 [ 0.85·f'c·(Ag − Ast) + fy·Ast ]     … ACI 318, 띠철근 기둥
φ       = 0.65 (설계 모드) / 1.00 (사용하중 모드)
Pu      = w × A_trib × (지지 층수)
```

### 1.7 하중

```
슬래브 자중  = 두께(m) × 24 kN/m³
드롭패널 자중 = (두께 × 24 × 드롭패널 면적) / 부담면적     … 등분포로 평균 환산
D = 슬래브 자중 + 드롭패널 자중 + 마감·설비 고정하중
설계 모드   : w = 1.2(D + 국부추가하중) + 1.6L,  φ = 0.75
사용하중 모드 : w = 1.0(D + 국부추가하중) + 1.0L,  φ = 1.00
```

- 국부 추가하중(설비·냉각탑·적치물)은 고정하중으로 취급합니다.
- 하중계수 1.2 / 1.6 은 ACI 318 및 KDS 41 의 일반적인 강도설계 조합을 따른 값입니다.

---

## 2. 수치의 출처 구분 (중요)

### 2.1 과제에서 제시된 조사자료 값 (그대로 사용)

| 변수 | Scenario A (시공 조건) | Scenario B (구조계산 조건) |
|---|---|---|
| 구조형식 | 무량판(드롭패널 없음) | 무량판(드롭패널 없음) |
| 기둥 직경 | 600 mm | 800 mm |
| 기둥 주철근 | 8-HD22 | 16-HD22 |
| 콘크리트 압축강도 | 18.4 MPa | 21 MPa |
| 유효깊이 | 약 360 mm | 약 410 mm |
| 5층 고정하중 | 용도 변경으로 증가 | 원설계 하중 |
| 국부 추가하중 | 존재 | 없음 |

### 2.2 확인하지 못해 **교육용 가정값**으로 둔 값

| 변수 | 기본값 | 왜 가정값인가 |
|---|---|---|
| 경간 (기둥 중심 간격) | 9,000 × 9,000 mm | 공개 자료에서 확정하지 못함 |
| 마감·설비 고정하중 | 판매장 1.5 / 식당가 4.5 kPa | 용도별 일반적 크기를 참고한 대표값 |
| 활하중 | 판매장·식당가 5.0 kPa | 용도별 설계 활하중 일반값 |
| 국부 추가하중 | 3.0 kPa | 설비·적치물을 가정한 크기 |

| 상부철근비 | 0.6 ~ 1.2 % | 확인하지 못함 (EC2 옵션에서만 사용) |
| 철근 항복강도 | 400 MPa | 일반적인 값 |

**경간 값에 대한 솔직한 설명**
처음에는 흔히 인용되는 10.8 m 를 기본값으로 두었지만, 그 경우 이 모형에서는
*원설계 조건(Scenario B)조차* 사용하중에서 D/C > 1 이 되어 "건물이 몇 년간 서 있었다"는
사실과 모순되었습니다. 그래서 다음 두 조건을 동시에 만족하는 값으로 9.0 m 를 선택했습니다.

1. 시공된 단면 + **원설계 하중** → 사용하중 D/C ≈ 0.93 (< 1.0, 건물이 서 있음)
2. 시공된 단면 + **용도 변경 하중 + 국부 추가하중** → 사용하중 D/C ≈ 1.28 (> 1.0)

즉 경간은 "붕괴 전후의 알려진 사실과 모순되지 않는 최소한의 가정"으로 고른 값이며,
**측정값이 아닙니다.** 화면의 슬라이더로 반드시 직접 바꿔 보고, 결론이 이 가정에 얼마나
민감한지 확인하는 것이 탐구의 중요한 부분입니다.

### 2.3 구조형식(드롭패널 유무)에 대한 기록 — 자료 충돌 있음

이 부분은 **자료가 서로 다르게 말하고 있어** 그대로 기록해 둡니다.

| 출처 | 내용 |
|---|---|
| 과제로 제시된 조사자료 요약 | "effective slab/**drop-panel** depth: 약 360 mm" — 드롭패널이 있다는 표현 |
| 사용자(작성자) 확인 | 삼풍백화점은 기둥과 슬래브 사이에 정사각형 판(드롭패널)이 **없는 무량판**이었다 |

**현재 모형은 사용자 확인을 따라 Scenario A·B 를 드롭패널이 없는 무량판(플랫플레이트)으로 두었습니다.**
따라서 검토하는 위험단면은 기둥 주변 하나뿐이고, 슬래브 두께는 유효깊이 + 40 mm 로 계산됩니다
(A: 400 mm, B: 450 mm). 드롭패널을 두었을 때보다 슬래브 자중이 커져 D/C 가 올라갑니다.

> **확인 필요 (TODO)** : 이 항목은 조사보고서나 Gardner 외(2002) 원문에서 반드시 다시 확인하세요.
> 드롭패널의 유무는 위험단면의 개수와 유효깊이를 동시에 바꾸는, 결과에 큰 영향을 주는 변수입니다.
> 화면의 "드롭패널" 토글로 두 경우를 직접 비교해 볼 수 있습니다.

---

## 3. 검증(validation) 기록용 표

기준 문헌:

> N.J. Gardner, Jungsuck Huh, Lan Chung,
> "Lessons from the Sampoong department store collapse",
> *Cement and Concrete Composites* **24** (2002) 523–529.

**이 프로그램은 위 논문의 원문 PDF 를 직접 대조하지 않았습니다.**
아래 표를 직접 채워 넣는 것이 이 탐구의 검증 단계입니다.
(논문은 학교 도서관 DB, DOI 검색, 또는 학술 데이터베이스로 확인할 수 있습니다.)

| 항목 | 논문에 제시된 값 | 이 프로그램의 값 | 차이와 그 이유 |
|---|---|---|---|
| 기둥 직경 | | 600 / 800 mm | |
| 콘크리트 압축강도 | | 18.4 / 21 MPa | |
| 슬래브·드롭패널 유효깊이 | | 360 / 410 mm | |
| 경간 | | 9,000 mm (가정) | |
| 고정하중 | | 계산됨 (하단 참조) | |
| 활하중 | | 5.0 kPa (가정) | |
| 구조형식 | | 무량판(드롭패널 없음) — 2.3절 참조 | |
| 소요 전단력 Vu | | Scenario A: 2,289 kN (계수) / 1,774 kN (사용) | |
| 설계 전단내력 φVn | | Scenario A: 1,044 kN (φ=0.75) / 1,391 kN (φ=1.0) | |
| 결론(D/C 또는 안전율) | | Scenario A: 2.19 (설계) / 1.28 (사용) | |

`npm run sanity` 를 실행하면 위 표를 채우는 데 필요한 중간값(둘레, 유효깊이, 하중, 전단력,
전단강도)이 모두 콘솔에 출력됩니다.

### 현재 모형의 출력 (2026년 기준, 기본 설정)

| | A. 삼풍 단순화 | B. 구조계산 | C. 안전 개선 |
|---|---|---|---|
| 구조형식 | 무량판 | 무량판 | 드롭패널 신설 |
| D/C (설계, 1.2D+1.6L, φ=0.75) | 2.19 | 1.18 | 0.47 |
| D/C (사용, 1.0D+1.0L, φ=1.0) | 1.28 | 0.67 | 0.27 |
| 지배 단면 | 기둥 주변 (d/2) | 기둥 주변 (d/2) | 기둥 주변 (d/2) |
| 기둥 축력 요구/저항 | 0.79 | 0.31 | 0.18 |

---

## 4. 내부 일관성 검증 (self-check)

`npm run sanity` 는 다음을 자동으로 확인합니다.

- 코드가 계산한 `vc` 와 손계산 `0.33·λs·√f'c` 가 일치하는지
- `b₀ = π(D + d)` 가 손계산과 일치하는지
- `V = w(A_trib − A_in)`, `vu = V/(b₀d)`, `D/C = vu/(φvn)` 의 연산 흐름이 화면 값과 같은지

이는 **모형 내부의 계산 오류를 잡는 것**이지, 모형이 현실을 맞게 표현한다는 증거가 아닙니다.

---

## 5. 역사 서술의 출처

`src/data/history.ts` 의 연표는 한국 근현대 건축·주거사에서 널리 알려진 사건을 요약한 것으로,
구조 계산 코드와 완전히 분리되어 있으며 계산에 전혀 사용되지 않습니다.
수행평가에 인용할 때는 교과서와 공신력 있는 자료로 다시 확인하세요.

---

## 6. TODO / 확신할 수 없어 남겨 둔 항목

공식이나 계수를 확신할 수 없는 부분은 임의로 만들지 않고 아래에 한계로 남겼습니다.
코드 안에서도 `TODO(한계)` 주석으로 표시되어 있습니다.

| 항목 | 현재 처리 | 왜 확신할 수 없는가 |
|---|---|---|
| **불균형 모멘트 전달** | Vu 에 곱하는 할증 계수(기본 1.00)로만 반영 | ACI 의 `γv`·`Jc` 계산에는 위험단면의 극관성모멘트와 접합부 모멘트가 필요한데, 이 모형은 슬래브를 해석하지 않아 접합부 모멘트를 모른다. 임의의 값을 쓰지 않고 사용자 입력으로 열어 두었다. |
| **전단보강량** | 0~1 의 상대 비율 | 실제 `vs = Av·fyt/(b₀·s)` 를 쓰려면 스터드 단면적·간격·열 수 입력이 필요하다. 교육용 단순화임을 화면에도 표시했다. |
| **EC2 전단보강식** | `0.75·v_Rd,c + ratio·v_Rd,c` | EC2 정식은 배근량이 필요하다. 이 식은 방향성만 보여 주는 근사이며 설계값이 아니다. |
| **원형 기둥 위험단면** | `b₀ = π(D + d)` | ACI 는 등가 정사각형 이상화도 허용한다. 두 방식의 결과가 몇 % 다르다. |
| **경간·하중 값** | 교육용 가정값 (2.2절) | 공개 자료에서 확정하지 못했다. |
| **드롭패널 치수** | 2,700 × 100 mm 가정 | 확인하지 못했다. |
| **연쇄붕괴 정량화** | 계산하지 않음 (3D 애니메이션은 개념 시각화) | 하중 재분배를 계산하려면 인접 부재를 포함한 해석 모델이 필요하다. |
| **1995년 당시 적용 기준** | 현행 ACI 318-19 / EC2 로 계산 | 당시 한국에서 적용된 기준의 조문을 확인하지 못했다. λs 를 끄면 1990년대식 `0.33√f'c` 형태에 가까워진다. |

---

## 7. "붕괴 회피 실험" 탭의 계산 방법

새로운 공식은 없습니다. 위 1장의 같은 식을 조건만 바꿔 반복 계산합니다.
구현 위치: `src/model/solver.ts`

### 7.1 2×2 요인 실험

변수를 두 묶음으로 나눕니다.

- **시공 조건** : 기둥 직경, 유효깊이, f'c, 주철근, 상부철근비, 드롭패널, 전단보강
- **하중 조건** : 용도, 마감·설비 고정하중, 활하중, 국부 추가하중

각 묶음을 `실제(Scenario A) / 원설계(Scenario B)` 두 가지로 두고 2 × 2 = 4 조합을 계산합니다.
"구조 때문인가, 사용 때문인가"를 분리해 보기 위한 설계입니다.

### 7.2 누적 개선 경로

Scenario A 에서 출발해 아래 순서로 조건을 하나씩 덮어씁니다. 각 단계마다 D/C 를 다시 계산합니다.
하중 쪽을 먼저 두어, 구조를 전혀 바꾸지 않아도 어디까지 내려가는지 볼 수 있게 했습니다.

```
① 국부 추가하중 제거 → ② 용도 원상복구 → ③ 기둥 800mm/16-HD22 → ④ d₁ 410mm
→ ⑤ f'c 21MPa → ⑥ 드롭패널 신설 3000×100 (d₁ +100) → ⑦ 전단보강 0.8
```

### 7.3 임계값 탐색

목표 D/C(1.00 또는 0.70)를 만족시키는 변수 하나의 최소 변화량을 찾습니다.

1. 현재값에서 슬라이더 한계값까지 120 등분해 훑어 목표를 처음 만족하는 구간을 찾고
2. 그 구간에서 이분법 40회로 임계값을 좁힙니다.

단조성을 가정하지 않기 위해 이분법 전에 구간 훑기를 먼저 합니다
(예: 유효깊이는 저항과 자중을 동시에 늘려 단조가 아닐 수 있음).
슬라이더 범위 안에서 목표에 도달할 수 없으면 "범위 내 불가능"으로 표시합니다.

### 7.4 이 실험으로 말할 수 있는 것과 없는 것

- 말할 수 있는 것 : **이 단순화 모형 안에서** 어떤 변화가 D/C 를 얼마나 움직이는가
- 말할 수 없는 것 : 실제 삼풍백화점이 그 조건에서 무너지지 않았을 것인지 여부.
  접합부 하나만 보고, 경간·하중이 가정값이며, 연쇄붕괴를 계산하지 않기 때문입니다.
